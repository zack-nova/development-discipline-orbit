# HUMANS.md - Development Discipline Orbit

这个 orbit 给想在代码仓库中保持小步、可验证开发纪律的人使用。

可用 skill：

- `diagnose`
- `review-commit`
- `tdd`

遇到 bug、失败、性能回退或根因不明的问题时使用 `diagnose`。
开发新行为、调整已有行为或希望走 red-green-refactor 时使用 `tdd`。
完成本地变更后，希望审查变更、记录非阻塞技术债并创建 commit 时使用 `review-commit`。
